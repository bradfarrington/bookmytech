// id of the homepage hero's reg input. The mobile sticky bar and the coverage
// section's "Check your postcode" button scroll to and focus it.
//
// A plain module (no "use client") so server components importing it get the
// string itself rather than a client reference.
export const GET_PRICE_INPUT_ID = "get-price";
